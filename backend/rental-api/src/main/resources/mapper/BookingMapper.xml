<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="run.frank.rentalapi.mapper.BookingMapper">

    <select id="findBookingsByOwnerId" resultType="run.frank.rentalapi.entity.Booking">
        SELECT b.*
        FROM booking b
        JOIN property p ON b.property_id = p.id
        WHERE p.owner_id = #{ownerId}
    </select>

    <select id="countConflictingConfirmedBookings" resultType="long">
        SELECT COUNT(*)
        FROM booking
        WHERE property_id = #{propertyId}
        AND status = 'CONFIRMED'
        AND start_date <![CDATA[ < ]]> #{endDate}
        AND end_date <![CDATA[ > ]]> #{startDate}
    </select>

</mapper>