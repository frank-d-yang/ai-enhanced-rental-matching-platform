<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="run.frank.rentalapi.mapper.PropertyMapper">
    <select id="searchPropertiesByLocation" resultType="run.frank.rentalapi.entity.Property">
        SELECT *,
               (
                   6371 * acos(
                           cos(radians(#{query.latitude})) *
                           cos(radians(latitude)) *
                           cos(radians(longitude) - radians(#{query.longitude})) +
                           sin(radians(#{query.latitude})) *
                           sin(radians(latitude))
                          )
                   ) AS distance
        FROM property
        WHERE status = 'PUBLISHED'
          AND latitude IS NOT NULL
          AND longitude IS NOT NULL
        ORDER BY distance ASC
    </select>
</mapper>